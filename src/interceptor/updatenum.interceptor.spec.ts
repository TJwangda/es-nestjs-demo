import { UpdatenumInterceptor } from './updatenum.interceptor';

describe('UpdatenumInterceptor', () => {
  it('should be defined', () => {
    expect(new UpdatenumInterceptor()).toBeDefined();
  });
});
